parameters:
  - name: yesNo
    type: string

steps:
  - checkout: none

  - bash: echo "Good"
    condition: eq('${{ parameters.yesNo }}', 'dev')

  - bash: echo "Bad"
    condition: eq('${{ parameters.yesNo }}', 'stg')