parameters:
  - name: yesNo
    type: boolean

steps:
  - checkout: none
  
  - bash: echo "Good"
    condition: eq(${{ parameters.yesNo }}, true)

  - bash: echo "Bad"
    condition: eq(${{ parameters.yesNo }}, false)