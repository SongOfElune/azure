trigger:
  branches:
    include:
      - master

pool:
  vmImage: "macOS-10.14"

variables:
  boolVar: true

jobs:
  - job: Run
    condition: in(variables['Build.Reason'], 'IndividualCI', 'Manual')
    strategy:
      maxParallel: 3
      matrix:
        dev:
          env: dev
        stg:
          env: stg
        uat:
          env: uat
    variables:
      env: 'dev'
    steps:
      - bash: echo "Good"
        condition: nin(variables['env'], 'dev', 'uat')
