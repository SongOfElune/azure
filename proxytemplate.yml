parameters:
  - name: project
    type: string
steps:
  - powershell: echo "this is proxy template"
  - template: pdtemplate.yml
    condition: eq(${{ parameters.project }}, 'PD')
  - template: platemplate.yml
    condition: eq(${{ parameters.project }}, 'PLA')

