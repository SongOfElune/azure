parameters:
- name: project
  type: string

steps:
  - powershell: echo "this is proxy template"
  - powershell: echo '${{ parameters.project }}'
  - ${{ if eq('${{ parameters.project }}', 'PD') }}:
    - template: pdtemplate.yml
  - ${{ if eq('${{ parameters.project }}', 'PLA') }}:
    - template: platemplate.yml
