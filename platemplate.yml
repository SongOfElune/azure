parameters:
  name: testParam
  type: object
  default:
    veracodeBranches:
      - branch0
      - branch1

steps:
  - powershell: echo "this is pla template"
  - bash: echo "${{ parameters.testParam.veracodeBranches }}"
