parameters:
  name: testParam
  type: object
  default:
    branches:
      - branch0
      - branch1

steps:
  - powershell: echo "this is pla template"
  - bash: echo "hey"
    condition: containsValue(${{ parameters.testParam }}, variables.SourceBranchName)
